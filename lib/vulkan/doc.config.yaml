name: AMEVA-Vulkan-Runtime
package_name_pypi: ameva-vulkan-runtime
package_name_npm: ameva-vulkan-runtime
version: "v1.2.1"
release_name: Production Release
license: Apache-2.0
platform: Android ARM64 / Snapdragon & Mali & Xclipse
github_repo_url: https://github.com/uno-km/ameva-vulkan-runtime
tagline_en: SoC-Aware Adaptive Abstraction Runtime Utilizing Device Resources for Android Termux
tagline_ko: 안드로이드 Termux 환경에서 디바이스 리소스를 활용하는 SoC 감지 및 적응형 추상화 런타임
quick_install_cmd: 'pip install ameva-vulkan-runtime

  # or: npm install ameva-vulkan-runtime'
why_challenge_en: Running multi-modal AI on mobile Android requires adaptive utilization
  of device resources across diverse SoC architectures (Qualcomm Adreno and Samsung Exynos ARM Mali).
why_challenge_ko: 안드로이드 환경에서 멀티모달 AI를 구동할 때 SoC 아키텍처(Qualcomm Adreno 및 Samsung Exynos Mali)에 따라 디바이스 리소스를 안정적으로 활용하는 것이 핵심 과제입니다.
description_en: Provides a C++20 Hardware Abstraction Layer (HAL) with Zero-Guesswork
  SoC auto-detection, utilizing hardware paths on Qualcomm Adreno and stable ARM NEON 4-Thread FP16 CPU execution on Exynos Mali.
description_ko: Zero-Guesswork SoC 자동 감지 기능을 갖춘 C++20 하드웨어 추상화 계층(HAL)을 통해 Qualcomm Adreno 환경에서는 하드웨어 경로를, Exynos Mali 환경에서는 안정적인 ARM NEON 4-스레드 FP16 CPU 연산을 지원합니다.
features:
- title_en: Runtime SoC Auto-Detection
  title_ko: SoC 런타임 자동 감지
  desc_en: Scans /proc/cpuinfo and device nodes on startup to route execution without guesswork.
  desc_ko: 시스템 구동 시 /proc/cpuinfo 및 디바이스 노드를 스캔하여 최적의 연산 경로로 자동 분기합니다.
- title_en: Adaptive Resource Routing
  title_ko: 적응형 디바이스 리소스 분기
  desc_en: Routes to hardware paths on Adreno and deterministic ARM NEON 4-Thread FP16 CPU execution on Exynos Mali.
  desc_ko: Adreno에서는 하드웨어 경로를 활용하고, Exynos Mali에서는 안정적인 ARM NEON 4-스레드 FP16 CPU 연산으로 즉각 분기합니다.
- title_en: Unified Resource Core
  title_ko: 공통 런타임 코어 일원화
  desc_en: Powers Whisper STT, LLaVA Vision, LLaMA/BitNet LLM, and Stable Diffusion from a single shared core.
  desc_ko: 단일 58MB 공유 라이브러리로 Whisper 음성인식, LLaVA 비전, LLaMA/BitNet LLM, Stable Diffusion의 디바이스 리소스 활용을 통합 관리합니다.
matrix_table:
- category: Target SoCs
  operations: Qualcomm Snapdragon (Adreno), Samsung Exynos (Cortex CPU NEON & Mali)
  status: Production
- category: Operation Modes
  operations: Hardware Execution (Adreno), ARM NEON 4-Thread FP16 CPU Mode (Exynos)
  status: Production
- category: Roadmap
  operations: Phase 1 CLI Deterministic Routing, Phase 2 Android Foreground Service JNI Wrapper
  status: Production
code_example_py: 'import ameva_vulkan_runtime as avr


  # 1. Probe & Validate Hardware (V0-V11)

  doctor = avr.Doctor()

  report = doctor.run_self_test()

  print(f"GPU Backend Status: {report.overall_success}, Device: {report.device_name}")


  # 2. Acquire High-Performance Vulkan Context for STT / LLM / Diffusion

  ctx = avr.create_context(device="auto", memory_limit_mb=1024)

  print(f"Context initialized via: {ctx.loader_path} (API {ctx.driver_version})")'
code_example_js: 'import { Doctor, createContext } from "ameva-vulkan-runtime";


  const doctor = new Doctor();

  const report = await doctor.runSelfTest();

  console.log(`GPU Status: ${report.overallSuccess}, GPU: ${report.deviceName}`);


  const ctx = await createContext({ device: "auto" });

  console.log(`Vulkan Context Ready on ${ctx.deviceName}`);'
changelog:
- version: "v1.2.1"
  date: '2026-09-01'
  title: Galaxy A35 Validation & CTypes Pointer Patch
  type: Production Release (Latest)
  changes:
  - 'Real-Device Galaxy A35 Validation: Certified 12-stage hardware diagnostic hierarchy
    and Vulkan 1.4 API negotiation on Samsung Galaxy A35 5G (Exynos 1380, ARM Mali-G68
    GPU).'
  - 'CTypes Struct Pointer Patch: Corrected struct pointer allocations to ctypes.pointer
    to fix TypeError in doctor.py.'
  - 'Dynamic Topology Thread Optimization: Added CPU topology inspection in LlamaCppAdapter
    targeting big-core clusters (-t 4) on mobile octa-core SoCs.'
  - 'Strict Vulkan 3-Tier Execution Mode: Fail-Fast protection for explicit --device
    vulkan requests without silent CPU masking.'
- version: "v1.2.1"
  date: '2026-08-15'
  title: Initial Production Release
  type: Initial Release
  changes:
  - Initial production release of ameva-vulkan-runtime.
  - 12-Stage Diagnostic Suite (Doctor) from V0 loader open to V11 model inference.
  - Multi-modality adapters for STT, TTS, Diffusion, BitNet, Vision, and LlamaCpp.
  - Cross-platform Bionic ICD driver loader (/system/lib64/libvulkan.so).
