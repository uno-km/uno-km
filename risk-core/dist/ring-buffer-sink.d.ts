import type { EventSink, StreamRecord, AsyncRingBufferOptions, RingBufferStats } from './types.js';
export declare class AsyncRingBufferSink implements EventSink {
    readonly name = "AsyncRingBufferSink";
    private readonly downstream;
    private readonly capacity;
    private readonly mask;
    private readonly flushIntervalMs;
    private readonly batchSize;
    private readonly overflowPolicy;
    private readonly circuitBreakerThreshold;
    private readonly circuitBreakerCooldownMs;
    private readonly onError;
    private readonly buffer;
    private head;
    private tail;
    private count;
    private droppedOldest;
    private droppedNewest;
    private circuitBreakerDrops;
    private failClosedRejects;
    private flushed;
    private flushFailures;
    private lastFlushTimestamp;
    private circuitBreakerState;
    private consecutiveFailures;
    private lastFailureTime;
    private flushTimer;
    private isClosed;
    private isFlushing;
    constructor(options: AsyncRingBufferOptions);
    private startTimer;
    private updateCircuitBreakerOnEnqueue;
    emit(record: StreamRecord): void;
    emitBatch(records: StreamRecord[]): void;
    flush(): Promise<void>;
    stats(): RingBufferStats;
    close(): Promise<void>;
}
